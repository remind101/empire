language: go

addons:
  postgresql: "9.3"

services:
  - postgresql

go:
  - 1.4

before_install:
  - go install -a -race std
  - go get github.com/tools/godep
  - export PATH=$HOME/gopath/bin:$PATH
  - make bootstrap

script:
  - go test -race ./...
